# Vendored shadcn-templ documentation

This directory is a verbatim offline snapshot of the upstream Markdown documentation for `github.com/axadrn/shadcn-templ/v2@v2.0.0-beta.3` (commit `e066f0ba1f3b690d5661647e74f313cde67f7a67`). It contains the core setup guides and all 52 component pages.

Read `../shadcn-templ.md` first for Guard-specific decisions, known compatibility boundaries, and a compact API digest. Use this snapshot when more component detail is needed. The upstream custom Markdown tags such as `ComponentPreview` and `ComponentSource` are documentation-site directives; they are not templ syntax to copy into Guard.

The upstream MIT license is included as `LICENSE`. Refresh this directory deliberately whenever the pinned module version changes.
